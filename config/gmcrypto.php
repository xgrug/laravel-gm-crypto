<?php

return [
    /*
    |--------------------------------------------------------------------------
    | Default Crypto Driver
    |--------------------------------------------------------------------------
    |
    | 默认使用的加密驱动 (sm3 或 sm4)
    |
    */
    'default' => env('GM_CRYPTO_DRIVER', 'sm4'),

    /*
    |--------------------------------------------------------------------------
    | SM4 Configuration
    |--------------------------------------------------------------------------
    |
    | SM4 对称加密配置
    |
    */
    'sm4' => [
        // 默认密钥（32位十六进制字符串，128位）
        'key' => env('GM_SM4_KEY', '0123456789abcdeffedcba9876543210'),

        // 加密模式: ECB, CBC
        'mode' => env('GM_SM4_MODE', 'CBC'),

        // CBC模式下的初始化向量（32位十六进制字符串）
        'iv' => env('GM_SM4_IV', 'fedcba98765432100123456789abcdef'),

        // 填充方式: pkcs7, zero, none
        // pkcs7: PKCS#7填充（标准，推荐）
        // zero: 零填充
        // none: 不填充（仅用于测试，数据必须是16字节的整数倍）
        'padding' => env('GM_SM4_PADDING', 'pkcs7'),
    ],

    /*
    |--------------------------------------------------------------------------
    | SM3 Configuration
    |--------------------------------------------------------------------------
    |
    | SM3 哈希算法配置
    |
    */
    'sm3' => [
        // 是否使用HMAC模式
        'hmac' => env('GM_SM3_HMAC', false),

        // HMAC密钥
        'hmac_key' => env('GM_SM3_HMAC_KEY', ''),
    ],
];